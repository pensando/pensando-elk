---
#
# Playbook to install the ELK stack for Pensando systems

- hosts: localhost
#   remote_user: "{{ ansible_system_user }}"
  vars_files:
    - roles/pensando-elk/vars/vars.yml
  roles:
    - { role: pensando-elk }